import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "QuickLend - Bridge USDC. Earn Bitcoin-backed Yields.",
  description:
    "The first cross-chain yield aggregator for USDCx on Stacks. Bridge your USDC from Ethereum and earn 8-12% APY via Zest Protocol.",
  keywords: ["DeFi", "USDC", "USDCx", "Stacks", "Bitcoin", "Yield", "Bridge"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
      </body>
    </html>
  );
}
